<div class="center"><div class="wizard">
    <a><span class="badge">1</span> Informar Dados</a>
    <a><span class="badge">2</span> Escolher Produtos</a>
    <a class="current"><span class="badge badge-inverse">3</span> Convidar Amigos</a>
</div></div>
<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :welcome_message %><br>
    <%= f.text_area :welcome_message %>
  </div>
  <div class="field">
    <%= f.label :thankyou_message %><br>
    <%= f.text_area :thankyou_message %>
  </div>
  <div class="field">
    <%= f.label :is_open %><br>
    <%= f.check_box :is_open %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
